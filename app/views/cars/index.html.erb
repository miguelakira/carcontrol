<p><h1><%= @mensagem %> </h1></p>
<% if !@cars.empty? %>

<%= render 'cars' %>

<%= hidden_field_tag :direction, params[:direction] %>
<%= hidden_field_tag :sort, params[:sort] %>
<%= will_paginate @cars %>

<br />
<% end -%>
<br />
<% if current_user.admin? %>
  <%= button('Novo Cliente (Pessoa Física)', new_car_path)%>
  <%= button('Novo Cliente (Pessoa Jurídica)', :action => :new, :pessoa_juridica => true)%>
<% end -%>
